apiVersion: v1
kind: Service
metadata:
  name: voting-result-svc
  labels:
    name: voting-result-svc
    app: voting-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
  selector:
    name: voting-result-pod
    app: voting-app
